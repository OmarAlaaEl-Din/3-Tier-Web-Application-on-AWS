plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  "tag:Project": novolt
keyed_groups:
  - key: tags.Role
    prefix: role
    separator: "_"
hostnames:
  - private-ip-address
compose:
  ansible_host: private_ip_address
